#! /bin/sh

case "$1" in
  deconfig)
    ;;
  bound)
    ifconfig ${interface} ${ip} netmask ${subnet}
    if [ "${interface}" = "eth0" ]; then
      route add -net default gw ${router}
    fi
    ;;
  renew)
    ;;
  nak)
    ;;
esac